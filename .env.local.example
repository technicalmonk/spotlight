# Database (Neon)
DATABASE_URL=postgres://user:password@host/db?sslmode=require

# Cron secret (generate a random string)
CRON_SECRET=your-secret-here

# OpenRouter (optional — API works without key, but rate-limited)
OPENROUTER_API_KEY=
